---
phase: 02-visual-grid-and-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/wordle_cli/__main__.py
  - src/wordle_cli/app.py
  - src/wordle_cli/widgets/__init__.py
  - src/wordle_cli/widgets/cell.py
  - src/wordle_cli/styles/app.tcss
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-06]

must_haves:
  truths:
    - "Empty cell displays with light gray border"
    - "Filled cell with CORRECT status shows green background"
    - "Filled cell with PRESENT status shows yellow background"
    - "Filled cell with ABSENT status shows gray background"
  artifacts:
    - path: "src/wordle_cli/widgets/cell.py"
      provides: "LetterCell widget with status-based styling"
      min_lines: 40
    - path: "src/wordle_cli/styles/app.tcss"
      provides: "Wordle color palette and cell styling"
      contains: "#6aaa64"
    - path: "src/wordle_cli/app.py"
      provides: "Textual App entry point"
      min_lines: 20
    - path: "pyproject.toml"
      provides: "Project dependencies including Textual"
      contains: "textual"
  key_links:
    - from: "src/wordle_cli/widgets/cell.py"
      to: "domain.models.LetterStatus"
      via: "CSS class mapping"
      pattern: "LetterStatus\\.(CORRECT|PRESENT|ABSENT)"
    - from: "src/wordle_cli/app.py"
      to: "widgets.cell.LetterCell"
      via: "import and composition"
      pattern: "from.*widgets.*import.*LetterCell"
---

<objective>
Establish Textual UI foundation with styled letter cells that display Wordle color feedback.

Purpose: Create the visual building blocks for the grid - individual cells that can show empty state or color-coded letter feedback matching NYTimes Wordle aesthetic.

Output: Working Textual app that renders LetterCell widgets with correct colors for each status (green/yellow/gray).
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md

# Domain models from Phase 1
@src/wordle_cli/domain/models.py
@src/wordle_cli/domain/__init__.py
</context>

<interfaces>
<!-- Key types from Phase 1 that this plan uses -->

From src/wordle_cli/domain/models.py:
```python
class LetterStatus(Enum):
    CORRECT = "correct"  # Green
    PRESENT = "present"  # Yellow  
    ABSENT = "absent"    # Gray

@dataclass(frozen=True)
class LetterResult:
    letter: str
    status: LetterStatus
    position: int
```

Textual patterns from research:
```python
# Widget with reactive attribute
from textual.reactive import reactive
from textual.widget import Widget

class MyWidget(Widget):
    value: reactive[str] = reactive("", recompose=True)
    
    def compose(self):
        yield Static(self.value)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Textual app scaffold with dependencies</name>
  <files>
    pyproject.toml
    src/wordle_cli/__main__.py
    src/wordle_cli/app.py
  </files>
  <action>
    Create project configuration with Textual dependencies and basic app entry point.

    1. Create pyproject.toml with:
       - name = "wordle-cli"
       - requires-python = ">=3.10"
       - dependencies = ["textual>=8.1.1"]
       - [project.scripts] wordle-cli = "wordle_cli.__main__:main"
       - [tool.pytest.ini_options] asyncio_mode = "auto" (for future Textual tests)
    
    2. Create src/wordle_cli/__main__.py:
       - def main() function that imports and runs WordleApp
       - if __name__ == "__main__": main()
    
    3. Create src/wordle_cli/app.py:
       - Import from textual.app import App
       - class WordleApp(App) with basic structure
       - CSS_PATH = "styles/app.tcss"
       - Empty compose() method (will add widgets in next task)
       - TITLE = "Wordle CLI"
    
    Use modern Python patterns consistent with Phase 1 (dataclasses, type hints).
  </action>
  <verify>
    <automated>python -c "from wordle_cli.app import WordleApp; app = WordleApp(); assert app.title == 'Wordle CLI'"</automated>
  </verify>
  <done>
    pyproject.toml exists with textual>=8.1.1 dependency
    __main__.py imports and creates WordleApp instance
    WordleApp class exists with CSS_PATH configured
    Import check passes without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LetterCell widget with color styling</name>
  <files>
    src/wordle_cli/widgets/__init__.py
    src/wordle_cli/widgets/cell.py
    src/wordle_cli/styles/app.tcss
    src/wordle_cli/app.py
  </files>
  <behavior>
    - Empty cell (letter="", status=None): Shows light gray border, no background, empty content
    - CORRECT cell (letter="W", status=CORRECT): Shows green background (#6aaa64), white text, bold
    - PRESENT cell (letter="O", status=PRESENT): Shows yellow background (#c9b458), white text, bold
    - ABSENT cell (letter="X", status=ABSENT): Shows gray background (#787c7e), white text, bold
    - Cell has consistent size and spacing (3x1 height/width for good visibility)
  </behavior>
  <action>
    Create LetterCell widget that displays a single letter with status-based styling.

    1. Create src/wordle_cli/widgets/__init__.py:
       - from .cell import LetterCell
       - __all__ = ["LetterCell"]
    
    2. Create src/wordle_cli/widgets/cell.py:
       - Import Static from textual.widgets
       - Import LetterStatus from wordle_cli.domain.models
       - class LetterCell(Static):
         - Accept letter: str = "", status: LetterStatus | None = None in __init__
         - Set renderable to uppercase letter (or empty for empty cells)
         - Add CSS classes based on status: "cell-correct", "cell-present", "cell-absent"
         - Always include base "letter-cell" class
         - Set classes in __init__ using self.add_class()
       - Make cell content centered and bold
    
    3. Create src/wordle_cli/styles/app.tcss with NYTimes Wordle colors:
       - .letter-cell: Base styling - border, padding, text-align center, min dimensions (width 5, height 3)
       - .letter-cell.cell-empty: Light gray border (#d3d6da), no background
       - .letter-cell.cell-correct: Green background (#6aaa64), white text, bold
       - .letter-cell.cell-present: Yellow background (#c9b458), white text, bold  
       - .letter-cell.cell-absent: Gray background (#787c7e), white text, bold
       - Screen: background $surface, layout vertical, align center middle
    
    4. Update src/wordle_cli/app.py compose() to show demo cells:
       - Import LetterCell from widgets
       - Import LetterStatus from domain.models
       - Yield Container with 4 demo cells showing all states:
         - LetterCell("", None) - empty
         - LetterCell("W", LetterStatus.CORRECT)
         - LetterCell("O", LetterStatus.PRESENT)
         - LetterCell("X", LetterStatus.ABSENT)
       - Use horizontal layout for demo (Horizontal container)
    
    Implementation notes:
    - Cell classes should be set in __init__, not in compose (Static doesn't re-compose)
    - Use uppercase() for letter display to match Wordle convention
    - Empty cell should have "cell-empty" class, others get status-specific class
    - Follow Textual reactive patterns from research (though this widget is static for now)
  </action>
  <verify>
    <automated>textual run --dev src/wordle_cli/app.py --screenshot .planning/phases/02-visual-grid-and-feedback/02-01-cells.svg && ls .planning/phases/02-visual-grid-and-feedback/02-01-cells.svg</automated>
  </verify>
  <done>
    LetterCell widget exists and can be imported
    TCSS file contains NYTimes Wordle color palette (#6aaa64, #c9b458, #787c7e)
    App displays 4 demo cells with correct colors (empty, green, yellow, gray)
    Screenshot shows visually correct rendering
  </done>
</task>

</tasks>

<verification>
Run the app and verify visual output:

```bash
textual run --dev src/wordle_cli/app.py
```

Expected display:
- App title "Wordle CLI" in header
- 4 cells in horizontal row: empty (gray border), W (green), O (yellow), X (gray)
- Clean NYTimes-style aesthetic
- Proper spacing and sizing

Manual checks:
- [ ] Empty cell shows light gray border, no background
- [ ] Green cell has #6aaa64 background
- [ ] Yellow cell has #c9b458 background
- [ ] Gray cell has #787c7e background
- [ ] Text is centered, bold, and readable
</verification>

<success_criteria>
- pyproject.toml exists with textual>=8.1.1 dependency
- WordleApp runs without errors via `textual run app.py`
- LetterCell widget displays letters with correct status colors
- TCSS stylesheet implements NYTimes Wordle color palette
- Demo app shows all 4 cell states (empty, correct, present, absent)
- Visual design matches expected Wordle aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-grid-and-feedback/02-01-SUMMARY.md`
</output>
