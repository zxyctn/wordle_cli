---
phase: 01-core-domain-and-game-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wordle_cli/domain/__init__.py
  - src/wordle_cli/domain/models.py
  - src/wordle_cli/domain/ports.py
autonomous: true
requirements:
  - GAME-01
  - GAME-05
  - GAME-07
  - GAME-08
  - LOGIC-03

must_haves:
  truths:
    - "Game state can represent 0-6 guess attempts"
    - "Letter evaluation results distinguish correct/present/absent states"
    - "Game state indicates win/loss/in-progress status"
    - "Word repository interface exists for domain logic to use"
  artifacts:
    - path: "src/wordle_cli/domain/models.py"
      provides: "Domain value objects for game state and results"
      min_lines: 60
      exports:
        - LetterStatus
        - LetterResult
        - GuessResult
        - GameState
    - path: "src/wordle_cli/domain/ports.py"
      provides: "Abstract interfaces for infrastructure dependencies"
      min_lines: 15
      exports:
        - WordRepository
  key_links:
    - from: "src/wordle_cli/domain/models.py"
      to: "enum.Enum"
      via: "LetterStatus enum definition"
      pattern: "class LetterStatus\\(Enum\\)"
    - from: "src/wordle_cli/domain/models.py"
      to: "dataclasses.dataclass"
      via: "frozen dataclasses for immutability"
      pattern: "@dataclass\\(frozen=True\\)"
---

<objective>
Establish the foundational domain models and port interfaces that define the game's core data structures and external dependencies.

Purpose: Create pure Python types that represent game state, letter evaluation results, and abstract interfaces. These models form the contract that all other components build upon. Using immutable dataclasses ensures predictable state management and thread safety.

Output: Domain layer foundation with zero external dependencies (including Textual). Models are testable, composable, and suitable for both TUI and potential future interfaces.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create domain models module</name>
  <files>src/wordle_cli/domain/__init__.py, src/wordle_cli/domain/models.py</files>
  <action>
Create `src/wordle_cli/domain/models.py` with the following components (use frozen dataclasses for immutability):

**LetterStatus enum:**
- Values: CORRECT (green - right letter, right position), PRESENT (yellow - right letter, wrong position), ABSENT (gray - not in word)
- String values: "correct", "present", "absent"

**LetterResult dataclass (frozen):**
- Fields: letter (str), status (LetterStatus), position (int, 0-4)
- Represents single letter evaluation result

**GuessResult dataclass (frozen):**
- Fields: guess (str), letters (list[LetterResult]), is_correct (bool)
- Represents full 5-letter guess evaluation

**GameState dataclass (frozen):**
- Fields: target_word (str), attempts (list[GuessResult], max 6), current_attempt (int, 0-6), is_won (bool), is_lost (bool)
- Property: is_game_over (bool) - returns True if is_won or is_lost
- Validation: Ensure current_attempt matches len(attempts)

Use Python 3.10+ type hints throughout. Import from enum, dataclasses, and typing only (standard library).

Create `src/wordle_cli/domain/__init__.py` that exports all public types: LetterStatus, LetterResult, GuessResult, GameState.
  </action>
  <verify>
    <automated>python -c "from src.wordle_cli.domain.models import LetterStatus, LetterResult, GuessResult, GameState; assert hasattr(GameState, 'is_game_over'); assert LetterStatus.CORRECT.value == 'correct'"</automated>
  </verify>
  <done>All domain models defined with correct types, models are frozen (immutable), LetterStatus enum has 3 values, GameState has is_game_over property</done>
</task>

<task type="auto">
  <name>Task 2: Create port interfaces</name>
  <files>src/wordle_cli/domain/ports.py, src/wordle_cli/domain/__init__.py</files>
  <action>
Create `src/wordle_cli/domain/ports.py` with abstract interface following hexagonal architecture pattern:

**WordRepository (ABC):**
- Abstract method: get_valid_words() -> list[str] — returns all valid 5-letter words
- Abstract method: is_valid_word(word: str) -> bool — checks if word exists in dictionary
- Abstract method: get_random_target() -> str — selects random word as target

Use abc.ABC and @abstractmethod decorators. Add clear docstrings explaining each method's contract.

Update `src/wordle_cli/domain/__init__.py` to also export WordRepository.
  </action>
  <verify>
    <automated>python -c "from src.wordle_cli.domain.ports import WordRepository; import inspect; assert inspect.isabstract(WordRepository); assert len([m for m in dir(WordRepository) if not m.startswith('_')]) == 3"</automated>
  </verify>
  <done>WordRepository abstract interface defined with 3 methods, properly uses ABC pattern, exported from domain package</done>
</task>

</tasks>

<verification>
1. All files created in correct directory structure (src/wordle_cli/domain/)
2. Models are immutable (frozen dataclasses)
3. LetterStatus enum has correct values
4. GameState has is_game_over computed property
5. WordRepository is abstract and has 3 required methods
6. No external dependencies (only Python stdlib)
7. All types properly exported from domain/__init__.py
</verification>

<success_criteria>
- Domain models exist and are importable
- Models use frozen dataclasses for immutability
- LetterStatus enum distinguishes three letter states
- GameState can represent game progress (0-6 attempts, win/loss status)
- WordRepository port interface exists for dependency inversion
- Zero dependencies on Textual or any external libraries
- All types follow Python 3.10+ conventions
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-domain-and-game-engine/01-01-SUMMARY.md`
</output>
