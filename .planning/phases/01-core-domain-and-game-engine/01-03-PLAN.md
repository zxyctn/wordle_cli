---
phase: 01-core-domain-and-game-engine
plan: 03
type: tdd
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/wordle_cli/domain/evaluator.py
  - src/wordle_cli/domain/game_engine.py
  - src/wordle_cli/domain/__init__.py
  - tests/test_domain/test_evaluator.py
  - tests/test_domain/test_game_engine.py
autonomous: true
requirements:
  - GAME-07
  - GAME-08
  - LOGIC-01
  - LOGIC-02
  - LOGIC-03
  - LOGIC-04

must_haves:
  truths:
    - "Evaluator correctly marks letters as CORRECT/PRESENT/ABSENT"
    - "Duplicate letters in guesses are independently evaluated"
    - "Duplicate letters in target words are handled correctly"
    - "Letter status priority (green > yellow > gray) is enforced"
    - "Game engine tracks 0-6 attempts and detects win/loss"
    - "Game validates words before processing guesses"
  artifacts:
    - path: "src/wordle_cli/domain/evaluator.py"
      provides: "Core guess evaluation logic with duplicate letter handling"
      min_lines: 60
      exports:
        - GuessEvaluator
    - path: "src/wordle_cli/domain/game_engine.py"
      provides: "Game orchestration and state management"
      min_lines: 80
      exports:
        - GameEngine
    - path: "tests/test_domain/test_evaluator.py"
      provides: "Comprehensive TDD tests for evaluation logic"
      min_lines: 150
      contains: "test_duplicate_letters"
    - path: "tests/test_domain/test_game_engine.py"
      provides: "Game engine behavior tests"
      min_lines: 100
      contains: "test_win_condition"
  key_links:
    - from: "src/wordle_cli/domain/game_engine.py"
      to: "src/wordle_cli/domain/ports.WordRepository"
      via: "dependency injection in __init__"
      pattern: "def __init__\\(self, word_repo: WordRepository\\)"
    - from: "src/wordle_cli/domain/game_engine.py"
      to: "src/wordle_cli/domain/evaluator.GuessEvaluator"
      via: "uses evaluator for guess comparison"
      pattern: "self\\._evaluator = GuessEvaluator\\(\\)"
    - from: "src/wordle_cli/domain/evaluator.py"
      to: "src/wordle_cli/domain/models.LetterStatus"
      via: "returns LetterResult with status"
      pattern: "LetterStatus\\.(CORRECT|PRESENT|ABSENT)"
---

<objective>
Implement the core game logic through TDD, focusing on correct letter evaluation (especially duplicate handling) and game state orchestration.

Purpose: Build the heart of Wordle - the evaluator that compares guesses against targets and the engine that orchestrates game flow. Using TDD ensures the notoriously tricky duplicate letter logic is bulletproof before any UI work begins.

Output: Fully tested game engine that validates words, evaluates guesses with correct duplicate letter handling, tracks game state, and detects win/loss conditions. Ready for UI integration in Phase 2.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
@./.opencode/get-shit-done/workflows/tdd-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md

# Depends on Plan 01 and 02 outputs
@src/wordle_cli/domain/models.py
@src/wordle_cli/domain/ports.py
@src/wordle_cli/adapters/word_repository.py
</context>

<feature name="GuessEvaluator">
  <files>src/wordle_cli/domain/evaluator.py, tests/test_domain/test_evaluator.py</files>
  
  <behavior>
**Critical: Duplicate letter handling**

Test case groups to implement:
1. **Simple cases (no duplicates):**
   - All correct: "WORLD" vs "WORLD" → all CORRECT
   - All wrong: "BRICK" vs "LOAMY" → all ABSENT
   - Mixed positions: "DROWN" vs "WORLD" → D=PRESENT, R=CORRECT, O=CORRECT, W=PRESENT, N=ABSENT

2. **Duplicate letters in guess (LOGIC-01, LOGIC-04):**
   - "ROBOT" (two Os) vs "WORLD" (one O) → first O=PRESENT, second O=ABSENT
   - "SPEED" (two Es) vs "CREEP" (three Es) → first E=PRESENT, second E=CORRECT
   - "LLAMA" (two Ls) vs "WORLD" (one L) → first L=CORRECT, second L=ABSENT

3. **Duplicate letters in target (LOGIC-02, LOGIC-04):**
   - "SWORD" (one O) vs "ROBOT" (two Os) → O=PRESENT
   - "LEVEL" (one L) vs "LLAMA" (two Ls) → L=CORRECT (position 0)
   - "TEPEE" vs "CREEP" → multiple E handling

4. **Priority rules (LOGIC-03):**
   - CORRECT takes priority over PRESENT for same letter in different positions
   - Each letter instance evaluated independently based on target letter availability
   - Greedy algorithm: assign CORRECT first, then PRESENT from remaining target letters

**Expected behavior:**
- `evaluate(guess: str, target: str) -> GuessResult`
- Returns GuessResult with 5 LetterResult objects
- Each LetterResult has: letter, status (CORRECT/PRESENT/ABSENT), position (0-4)
- Algorithm: First pass marks CORRECT positions, second pass marks PRESENT from remaining target letters, rest ABSENT
  </behavior>
  
  <implementation>
After tests pass, implement GuessEvaluator:

**Algorithm (two-pass to handle duplicates correctly):**
1. Create target letter frequency map
2. First pass: Mark CORRECT positions, decrement frequency
3. Second pass: Mark PRESENT if letter available in frequency map (decrement), else ABSENT
4. Return GuessResult with all LetterResult objects

**Class structure:**
```python
class GuessEvaluator:
    def evaluate(self, guess: str, target: str) -> GuessResult:
        # Implementation here
        pass
```

Use LetterStatus enum, LetterResult, and GuessResult from models module.
  </implementation>
</feature>

<feature name="GameEngine">
  <files>src/wordle_cli/domain/game_engine.py, tests/test_domain/test_game_engine.py</files>
  
  <behavior>
**Game orchestration test cases:**

1. **Initialization (GAME-01, GAME-07):**
   - `start_new_game()` selects random target from WordRepository
   - Initial state: 0 attempts, current_attempt=0, not won, not lost

2. **Word validation (GAME-05, GAME-06):**
   - `process_guess("world")` → valid word processed
   - `process_guess("xyzqk")` → raises ValueError with message "Invalid word"

3. **Guess processing:**
   - Valid guess increments current_attempt
   - Guess added to attempts list
   - Returns updated GameState

4. **Win condition (GAME-08):**
   - Correct guess sets is_won=True, is_game_over=True
   - No more guesses accepted after win

5. **Loss condition (GAME-07, GAME-08):**
   - 6 incorrect guesses sets is_lost=True, is_game_over=True
   - Attempt count maxes at 6

6. **Edge cases:**
   - Case insensitive: "WORLD" and "world" both valid
   - Cannot process guess after game over
   - Empty or wrong-length guesses raise ValueError

**Expected interface:**
- `__init__(word_repo: WordRepository, seed: int | None = None)`
- `start_new_game() -> GameState`
- `process_guess(guess: str) -> GameState`
- `get_current_state() -> GameState`
  </behavior>
  
  <implementation>
After tests pass, implement GameEngine:

**Class structure:**
```python
class GameEngine:
    def __init__(self, word_repo: WordRepository, seed: int | None = None):
        self._word_repo = word_repo
        self._evaluator = GuessEvaluator()
        self._state: GameState | None = None
        # If seed provided, set random seed for reproducibility
    
    def start_new_game() -> GameState:
        # Select random target via word_repo.get_random_target()
        # Initialize GameState with empty attempts
        # Return initial state
    
    def process_guess(guess: str) -> GameState:
        # Validate: game not over, guess length = 5, word valid
        # Evaluate guess using _evaluator
        # Update state (add to attempts, increment current_attempt)
        # Check win/loss conditions
        # Return new immutable GameState
    
    def get_current_state() -> GameState:
        # Return current _state
```

Use dependency injection for WordRepository. Keep state immutable - create new GameState on each update.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD GuessEvaluator - Simple cases (RED)</name>
  <files>tests/test_domain/test_evaluator.py</files>
  <behavior>
    - Test: All letters correct → all LetterStatus.CORRECT
    - Test: No letters match → all LetterStatus.ABSENT
    - Test: Mixed positions → correct CORRECT/PRESENT/ABSENT mix
  </behavior>
  <action>
Create `tests/test_domain/test_evaluator.py` with pytest tests for simple evaluation cases (no duplicate letters). Write failing tests first:

1. `test_all_correct()`: "WORLD" vs "WORLD" → 5 CORRECT
2. `test_all_absent()`: "BRICK" vs "LOAMY" → 5 ABSENT
3. `test_mixed_positions()`: "DROWN" vs "WORLD" → verify each letter's status

Run tests - they should FAIL (GuessEvaluator doesn't exist yet).

Then create minimal `src/wordle_cli/domain/evaluator.py` with GuessEvaluator stub that makes tests pass. Use two-pass algorithm.

Commit: `test(01-03): add GuessEvaluator simple cases tests (RED)`
Commit: `feat(01-03): implement GuessEvaluator simple cases (GREEN)`
  </action>
  <verify>
    <automated>pytest tests/test_domain/test_evaluator.py::test_all_correct tests/test_domain/test_evaluator.py::test_all_absent tests/test_domain/test_evaluator.py::test_mixed_positions -v</automated>
  </verify>
  <done>Simple evaluation tests pass, evaluator handles non-duplicate cases correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD GuessEvaluator - Duplicate letter handling (RED→GREEN)</name>
  <files>tests/test_domain/test_evaluator.py, src/wordle_cli/domain/evaluator.py</files>
  <behavior>
    - Test: Duplicate in guess, single in target → first marked correctly, second ABSENT
    - Test: Multiple in both → frequency-based matching
    - Test: Priority rules → CORRECT before PRESENT
  </behavior>
  <action>
Add comprehensive duplicate letter tests to test_evaluator.py:

**Duplicate in guess (LOGIC-01, LOGIC-04):**
3. `test_duplicate_in_guess_single_in_target()`: "ROBOT" vs "WORLD" → R=CORRECT, O=CORRECT (pos 1), B=ABSENT, O=ABSENT (pos 3), T=ABSENT
4. `test_multiple_same_letter_in_guess()`: "SPEED" vs "CREEP" → S=ABSENT, P=PRESENT, E=PRESENT (pos 2), E=CORRECT (pos 3), D=ABSENT
5. `test_duplicate_L_in_guess()`: "LLAMA" vs "WORLD" → L=CORRECT (pos 0), L=ABSENT (pos 1), A=ABSENT, M=ABSENT, A=ABSENT

**Duplicate in target (LOGIC-02):**
6. `test_single_in_guess_duplicate_in_target()`: "SWORD" vs "ROBOT" → verify O marked appropriately
7. `test_complex_duplicate_scenario()`: "TEPEE" vs "CREEP" → verify all E evaluations

**Priority (LOGIC-03):**
8. `test_correct_priority_over_present()`: Design scenario where same letter appears twice in guess, once matches position (CORRECT), once doesn't (should be ABSENT, not PRESENT, if target only has one)

Run tests - they should FAIL initially. Update GuessEvaluator implementation to handle duplicates with two-pass algorithm (CORRECT first, then PRESENT from remaining). Refactor if needed.

Commit: `test(01-03): add duplicate letter evaluation tests (RED)`
Commit: `feat(01-03): handle duplicate letters correctly (GREEN)`
Commit (if needed): `refactor(01-03): clean up evaluator algorithm`
  </action>
  <verify>
    <automated>pytest tests/test_domain/test_evaluator.py -v</automated>
  </verify>
  <done>All duplicate letter tests pass, evaluator correctly implements LOGIC-01 through LOGIC-04, two-pass algorithm verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TDD GameEngine - Core game flow (RED→GREEN)</name>
  <files>tests/test_domain/test_game_engine.py, src/wordle_cli/domain/game_engine.py, src/wordle_cli/domain/__init__.py</files>
  <behavior>
    - Test: start_new_game creates initial state
    - Test: Valid guess increments attempts
    - Test: Invalid word raises error
    - Test: Win condition detected
    - Test: Loss condition after 6 attempts
    - Test: Cannot guess after game over
  </behavior>
  <action>
Create `tests/test_domain/test_game_engine.py` with GameEngine tests:

**Setup:** Use InMemoryWordRepository with fixed seed for reproducible tests

**Test cases:**
1. `test_initialization()`: Create engine, verify no game started yet
2. `test_start_new_game()`: Call start_new_game(), verify initial GameState (0 attempts, not won/lost)
3. `test_process_valid_guess()`: Process valid guess, verify attempts increment, state updated
4. `test_reject_invalid_word()`: Process "xyzqk", verify ValueError raised
5. `test_win_condition()`: Feed target word as guess, verify is_won=True, is_game_over=True
6. `test_loss_condition()`: Process 6 wrong guesses, verify is_lost=True, is_game_over=True
7. `test_cannot_guess_after_win()`: Win game, try another guess, verify error
8. `test_cannot_guess_after_loss()`: Lose game, try another guess, verify error
9. `test_case_insensitive_guess()`: Process "WORLD" and "world", both should work

Write tests first (RED), then implement GameEngine in `src/wordle_cli/domain/game_engine.py` (GREEN). Use GuessEvaluator and WordRepository.

Update `src/wordle_cli/domain/__init__.py` to export GuessEvaluator and GameEngine.

Commit: `test(01-03): add GameEngine tests (RED)`
Commit: `feat(01-03): implement GameEngine orchestration (GREEN)`
  </action>
  <verify>
    <automated>pytest tests/test_domain/test_game_engine.py -v</automated>
  </verify>
  <done>GameEngine tests pass, word validation works, win/loss conditions detected, game state correctly tracked through 6 attempts</done>
</task>

</tasks>

<verification>
1. All test files exist and are comprehensive
2. GuessEvaluator correctly handles all duplicate letter scenarios
3. GameEngine validates words before processing
4. Win condition (correct guess) sets is_won=True
5. Loss condition (6 wrong guesses) sets is_lost=True
6. Game rejects guesses after game over
7. All tests pass (pytest reports 100% pass rate)
8. Code follows two-pass algorithm for duplicate handling
9. Domain layer has zero dependencies on Textual or external libraries
10. All domain types exported from domain/__init__.py
</verification>

<success_criteria>
- GuessEvaluator correctly evaluates letters (CORRECT/PRESENT/ABSENT)
- Duplicate letters in guesses handled independently (LOGIC-01, LOGIC-04)
- Duplicate letters in target words handled correctly (LOGIC-02)
- Letter status priority enforced (green > yellow > gray) (LOGIC-03)
- GameEngine tracks attempts and detects win after correct guess (GAME-08)
- GameEngine detects loss after 6 failed attempts (GAME-07, GAME-08)
- GameEngine validates words before processing (GAME-05, GAME-06)
- All tests pass with comprehensive coverage
- TDD workflow followed (RED→GREEN→REFACTOR cycles)
- Code is pure Python with no UI dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-domain-and-game-engine/01-03-SUMMARY.md`
</output>
